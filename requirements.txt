# Core: Gemma 4 (E2B / E2B-it) requires a recent transformers build
# (Gemma 4 support landed in transformers only recently — if you hit a
# "model type gemma4 not recognized" error, install from source instead:
#   pip install git+https://github.com/huggingface/transformers.git
transformers>=4.56.0
torch>=2.4.0
accelerate>=0.33.0

# Hugging Face Hub auth + model/dataset download
huggingface_hub>=0.24.0

# Multimodal support (Gemma 4 handles image / video / audio)
pillow>=10.0.0
# av>=12.0.0          # video decoding
# soundfile>=0.12.1   # audio I/O
# librosa>=0.10.1     # audio preprocessing

# Faster inference (optional but recommended if you have a CUDA GPU)
# bitsandbytes>=0.43.0   # 4-bit/8-bit quantization for lower VRAM use
# flash-attn>=2.6.0      # uncomment only if your GPU/driver supports it

# Common hackathon utilities
numpy>=1.26.0
pandas>=2.2.0
tqdm>=4.66.0
python-dotenv>=1.0.0    # for keeping your HF token out of source control

